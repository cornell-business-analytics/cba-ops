You are making a single, focused design change to the CBA public website (apps/website/).

Read the CLAUDE.md in apps/website/ first — it has the file map, color system, and layout patterns.

Steps:
1. Identify which file(s) need to change based on the request.
2. Read those files before editing so you understand the existing code.
3. Make only the requested change. Do not refactor surrounding code.
4. Only edit files under apps/website/. Never touch apps/backend, apps/frontend, packages/, or .github/.
5. If the change is impossible without backend work, leave files unmodified and explain why.

Image notes:
- If the description says an image was attached or uploaded, it has already been placed at the correct path inside apps/website/public/ before you ran. You do not need to move or download it — just reference it from JSX using that path (e.g. /logos/bain.png).
- For an image swap at the same path, no JSX changes are needed — the file was overwritten in place.
- For a new image at a new path, write the JSX to display it at the specified location.

Requested change:
